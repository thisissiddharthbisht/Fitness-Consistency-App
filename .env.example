# Frontend API URL (optional locally; production falls back to Render automatically).
VITE_API_BASE_URL=http://127.0.0.1:8000

# Vercel: set VITE_API_BASE_URL=https://fitness-consistency-api.onrender.com
# then redeploy so builds pick up the env var explicitly.
